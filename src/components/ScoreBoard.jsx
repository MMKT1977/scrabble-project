// this component keeps track of each player scores, it will useState to update the
//  scores and also to keep track of the player turns so as to avoid updating the wrong player scores

import React from "react";

function ScoreBoard(){
    return (
        <div>
            
        </div>
    );
}

export default ScoreBoard;